<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prismgl.renderer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <uses-feature android:glEsVersion="0x00030002" android:required="true" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.PrismGL"
        android:extractNativeLibs="true"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Content provider for launcher integration -->
        <provider
            android:name=".RendererProvider"
            android:authorities="com.prismgl.renderer.provider"
            android:exported="true"
            android:grantUriPermissions="true" />

        <!-- Metadata for launcher detection -->
        <meta-data
            android:name="com.prismgl.renderer.name"
            android:value="PrismGL" />
        <meta-data
            android:name="com.prismgl.renderer.version"
            android:value="1.0.0" />
        <meta-data
            android:name="com.prismgl.renderer.gl_version"
            android:value="4.6" />
        <meta-data
            android:name="com.prismgl.renderer.library"
            android:value="libPrismGL.so" />
    </application>
</manifest>
